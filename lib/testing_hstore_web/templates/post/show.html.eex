<h2>Show Post</h2>

<ul>
  <%= for item <- @post.list do  %>
    <li><%= item["foo"] %> - <%= item["bar"] %></li>
  <% end %>
</ul>

<span><%= link "Edit", to: post_path(@conn, :edit, @post) %></span>
<span><%= link "Back", to: post_path(@conn, :index) %></span>
